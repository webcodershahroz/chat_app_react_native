import React, {useLayoutEffect, useRef, useState} from 'react';
import {
  Text,
  StyleSheet,
  TextInput,
  View,
  Pressable,
  Image,
  SafeAreaView,
  ScrollView,
  Dimensions,
  Keyboard,
  KeyboardAvoidingView,
} from 'react-native';

function ThemeSettingScreen({navigation, route}) {

  return (
    <>
      <Text style={{color:'black',textAlign:'center'}}>Theme will be added soon</Text>
    </>
  );
}

const styles = StyleSheet.create({
 
});

export default ThemeSettingScreen;
